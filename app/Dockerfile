# Instructions to build a Docker image for the Hivebox application
FROM python:3.11-slim

# Add the application code to the container
ADD app.py .

# Command to run the application
CMD ["python", "./app.py"]
